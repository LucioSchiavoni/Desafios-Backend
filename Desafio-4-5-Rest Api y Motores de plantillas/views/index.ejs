<!doctype html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/dist/output.css" rel="stylesheet">
</head>

<body>
    <tbody>
        <% productServices.forEach((user)=> { %>
            <tr>
                <%= user.id %>
            </tr>
            <tr>
                <%= user.nombre %>
            </tr>
            <tr>
                <%= user.edad %>
            </tr>
            <tr>
                <%= user.comentario %>
            </tr>
            <tr>
                <%= user.puesto %>
            </tr>
            <% }) %>
    </tbody>
</body>

</html>